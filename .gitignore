*.iml
.gradle
/local.properties
/.idea
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties
*.apk
*.aab
*.keystore
!debug.keystore
keystore.properties
*.jks

# GEBCO MBTiles (large generated files, not tracked in repo)
# These are needed locally for APK builds but must not be committed.
*.mbtiles
